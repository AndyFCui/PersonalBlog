# Security

## 原则
安全不是附属品，是默认设计约束。

## 输入安全

- 所有外部输入默认不可信
- 做参数校验、长度限制、格式限制
- 不信任前端，不信任 URL，不信任第三方回调
- 对富文本、HTML、脚本内容做严格处理

## 权限控制

- 先认证，再鉴权，再执行
- 默认最小权限
- 不允许“只在前端控制权限”
- 管理员能力必须明确隔离

## 数据安全

- 敏感信息不写日志
- 密钥、Token、密码不得硬编码
- 不在客户端暴露服务端密钥
- 对个人信息、支付信息、鉴权信息做脱敏处理

## 数据库安全

- 使用参数化查询
- 禁止字符串拼接 SQL
- 高风险写操作要有事务保护
- 删除 / 更新必须有明确条件，不允许裸写

## 网络与接口

- 所有写操作考虑 CSRF / 重放 / 幂等
- Webhook 必须校验签名
- 上传接口必须限制类型、大小、扩展名和内容
- 下载接口必须校验访问权限

## 日志与可观测性

- 错误日志要可排查，但不可泄漏敏感信息
- 安全事件应有审计信息
- 对登录失败、权限拒绝、异常流量做监控

## 高风险清单

遇到以下内容，必须主动提醒风险：
- 文件上传
- 支付 / 扣费
- 权限提升
- 删除数据
- 执行脚本
- 反序列化
- Webhook
- OAuth / 登录态
- 多租户数据隔离

## 输出要求

当任务涉及安全敏感点时，默认补充：
1. 攻击面是什么
2. 目前缺什么防线
3. 最小修复措施是什么
4. 是否还有更稳妥的结构性方案
