# API Conventions

## 目标
接口要稳定、直白、可预测。

## 基本规范

- URL 表达资源，不表达动作堆砌
- HTTP 方法语义清晰
- 请求和响应结构稳定
- 错误码和错误信息可消费
- 不把后端内部细节泄漏给前端

## 路由设计

推荐：
- GET /users/:id
- POST /orders
- PATCH /orders/:id
- DELETE /orders/:id

避免：
- POST /getUser
- POST /deleteOrder
- POST /updateEverything

## 请求体

- 字段命名统一
- 不混用 snake_case 和 camelCase
- 可选字段必须定义清楚默认行为
- 时间、金额、状态字段必须明确格式

## 响应体

推荐结构：

```json
{
  "data": {},
  "error": null,
  "meta": {}
}
```

错误响应推荐：

```json
{
  "data": null,
  "error": {
    "code": "ORDER_NOT_FOUND",
    "message": "订单不存在"
  },
  "meta": {}
}
```

## 状态码

- 200: 成功读取 / 成功更新
- 201: 创建成功
- 400: 参数错误
- 401: 未认证
- 403: 已认证但无权限
- 404: 资源不存在
- 409: 资源冲突
- 422: 语义合法性失败
- 500: 服务端异常

## 幂等性

以下操作必须明确讨论幂等性：
- 创建
- 扣费
- 提交
- 重试
- 回调

必要时：
- 使用 idempotency key
- 使用去重键
- 使用状态机保护重复写入

## 分页

统一约定：
- cursor-based 优先于 page-based
- 响应中返回 next_cursor
- 不把分页细节散落到多个字段风格中

## 版本控制

- 有破坏性变更时必须标明版本策略
- 不允许静默修改字段语义
- 弃用接口需给迁移说明

## 输出要求

当任务涉及 API 设计时，必须回答：
1. 资源模型是什么
2. 路由为什么这样设计
3. 错误语义是否清晰
4. 幂等性怎么保证
5. 是否存在兼容性风险
